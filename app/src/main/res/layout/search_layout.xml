<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    >
    <TableLayout android:layout_width="fill_parent"
        android:layout_height="wrap_content">
        <TableRow>
            <TextView android:text="編號" />
            <EditText android:id="@+id/edtID" android:width="60dp"/>
            <Button android:id="@+id/btnSaerch" android:text="查詢"/>
            <Button android:id="@+id/btnSearchAll" android:text="查詢全部" />
        </TableRow>
    </TableLayout>
    <TableLayout android:layout_width="fill_parent"
        android:layout_height="388dp">
        <TableRow>
            <ListView android:id="@+id/ListView01" android:layout_weight="1"/>
        </TableRow>
    </TableLayout>
    <FrameLayout
        android:id="@+id/sample_content_fragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
    </FrameLayout>
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="back"
            android:id="@+id/back_button"
            android:layout_gravity="right|bottom" />
    </FrameLayout>
</LinearLayout>